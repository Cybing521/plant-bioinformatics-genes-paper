# 植物"序列→表达"预测 依赖清单
# 注意：AutoDL PyTorch 镜像已自带 torch+CUDA，setup_env.sh 会跳过 torch 安装；
# 如需全新环境，请手动安装对应 CUDA 版 torch（见 setup_env.sh）。

# --- 数据处理 ---
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65
pyfaidx>=0.7.2.2
biopython>=1.81

# --- 深度学习（不含 torch 本体）---
# 注意: 版本锁定与 torch 2.4.1 兼容（transformers/datasets 5.x 需要 torch>=2.5）
transformers==4.44.2
peft==0.12.0
datasets==2.21.0
accelerate==0.34.2

# --- 可解释性 ---
shap>=0.44
captum>=0.7.0          # DeepLift 归因（shap DeepExplainer 在 0.44 有 bug）
modisco-lite>=1.1.0
logomaker>=0.8
h5py>=3.9
hdf5plugin>=4.3
